thought
To optimize BFS over a large sparse graph, use a `collections.deque` as the BFS queue to ensure constant-time `popleft` and `append` operations, effectively managing the frontier without the overhead of shifting elements in a list.

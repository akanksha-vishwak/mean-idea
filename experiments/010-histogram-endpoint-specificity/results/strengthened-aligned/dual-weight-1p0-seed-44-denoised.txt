thought
To improve performance, use `bytes.count` or `numpy.histogram` within each worker to generate private partial histograms from disjoint chunks, significantly reducing the overhead of manual Python loops before performing the parent sum.

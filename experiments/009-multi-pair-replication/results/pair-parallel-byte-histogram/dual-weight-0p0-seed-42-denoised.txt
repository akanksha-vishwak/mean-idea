thought
To improve performance on large buffers, use `np.bincount` on a `uint8` view created via `np.frombuffer` with `minlength=256` to ensure a vectorized, zero-copy computation of the byte-frequency histogram.

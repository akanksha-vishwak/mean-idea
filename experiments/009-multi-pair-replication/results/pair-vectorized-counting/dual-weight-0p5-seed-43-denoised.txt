thought
Optimize `my_sort` by implementing a **counting sort** that utilizes a **fixed frequency array** to count occurrences of integers **0-9999**, reconstructing the sorted output through a single pass of the counts.

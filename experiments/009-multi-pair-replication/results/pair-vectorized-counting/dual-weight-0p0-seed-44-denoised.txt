thought
Implement `my_sort` by using a **counting sort** with a **fixed frequency array** of size 10,000 to store occurrences of integers **0-9999**, then reconstruct the sorted list by iterating through the counts and appending each index to the output.
